@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2 style="color:red">Prefered Products of the week </h2>
<h4 style="color:red">Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var drink in Model.PreferredDrinks)
    {
        @Html.Partial("ProductsSummary", drink)
    }
</div>